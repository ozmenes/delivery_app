import 'package:flutter/material.dart';

class DeliveryFilter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('DeliveryFilter..'),
    );
  }
}

class PickUpFilter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PickUpFilter..'),
    );
  }
}
